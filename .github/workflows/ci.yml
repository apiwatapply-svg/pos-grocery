name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            front-end/package-lock.json
            back-end/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefix front-end

      - name: Install backend dependencies
        run: npm ci --prefix back-end

      - name: Lint
        run: npm run lint

      - name: Prisma validate
        run: npm run prisma:validate
        env:
          PRISMA_DATABASE_URL: file:./dev.db

      - name: Prisma generate
        run: npm run prisma:generate
        env:
          PRISMA_DATABASE_URL: file:./dev.db

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Build frontend
        run: npm --prefix front-end run build

      - name: Build backend
        # Skip the prebuild migration step (requires DATABASE_URL) and
        # run the typecheck/Prisma generate directly. This mirrors what
        # deploy.yml already does for the backend.
        run: npm --prefix back-end run prisma -- generate && npm --prefix back-end run typecheck
        env:
          PRISMA_DATABASE_URL: file:./dev.db

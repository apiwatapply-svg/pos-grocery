# Render sets NODE_ENV=production in the build step, which makes
# `npm install` skip devDependencies by default. The back-end build
# still needs `@types/*` for `tsc --noEmit`, so always include dev
# dependencies on install.
include=dev
